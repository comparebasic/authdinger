Hi {fullname},
{token} is your {brand} login code, or click the link <a href="{url}/auth/token-login?token={token}&email={email-token}&redir=/auth/dashboard">{url}/auth/token-login?token={token}&email={email-token}&redir=/auth/dashboard</a>, to login.
